# Principal Engineer Review — 2026-04-10T15:22 UTC

## Scope

Full repo review: `index.html`, `server.py`, `send.py`, `readme.md`, and all docs under `docs/`.

---

## Overall Assessment

Codebase is in good shape. No logic errors, no dead functions, no unused parameters, no orphaned code. All roadmap items marked as checked were verified as correctly implemented. Architecture is sound and consistent with the documented design.

The five findings below were all fixed in commit `afe3504`.

---

## Findings & Fixes

### P0 — Critical: `send.py` duplicate content
**File:** `send.py`, lines 171–284  
**Issue:** An older, simpler version of the file (4 commands: behavior, param, look, blink) was appended verbatim after the full rewrite (11 commands). Python would parse only the first `if __name__ == "__main__"` block; the duplicate was dead code but extremely confusing and would mislead anyone reading the file.  
**Fix:** Deleted lines 171–284 (the duplicate). ✓

---

### P1 — Bug: `const t` variable shadow in `getCompositor()`
**File:** `index.html`, `getCompositor()` function  
**Issue:** `const t = state.transBlend;` was declared on one line, then `const t = state.transEaseFn ? state.transEaseFn(state.transBlend) : state.transBlend;` was declared two lines later. In strict mode this is a syntax error (`const` in the same scope); in non-strict mode the first declaration is dead. The eased version was always the intended one.  
**Fix:** Removed the dead first declaration; kept only the eased `t`. ✓

---

### P2 — Docs: `behaviors.md` missing two transTable entries
**File:** `docs/behaviors.md`, pair-specific transition overrides table  
**Issue:** `transTable` in `index.html` has 6 entries; the doc listed only 4. Missing:  
- `waking → interrupted: 0.08s` (still-groggy startle)  
- `resting → attentive: 0.35s` (slow rise to attention)  
**Fix:** Added both entries to the table. ✓

---

### P3 — Docs: `drive-system.md` overstated driveProfile coverage
**File:** `docs/drive-system.md`, "Drive layer (baseline)" section  
**Issue:** The doc described the drive profile as if all states had a resting pressure target. In reality only `idle` (0.40) and `curious` (0.25) have non-zero baselines; the other 13 default to 0 and are purely reactive to external stimulus via the rule engine.  
**Fix:** Rewrote the paragraph to state this explicitly, including that the profile can be extended. ✓

---

### P4 — Docs: Behavior count wrong across multiple files
**Files:** `readme.md`, `docs/testing.md` (3 locations)  
**Issue:** Behavior count stated as 15. Actual count is 17:  
Classic (4) + Attention (2) + Conversational (4) + Affective (4) + Operational (3) = 17.  
The JS unit test example also asserted `toHaveLength(15)`.  
**Fix:** Updated all occurrences to 17. ✓

---

## Other Notes

- `server.py` has an undocumented `config` WebSocket command in its handler that `send.py` doesn't expose. Low risk (no external surface), but worth documenting or wrapping in a `send.py config <key> <value>` command if it proves useful.
- `index.html` is 3,500+ lines. No immediate action needed, but when the engine extraction roadmap item is tackled (splitting into `engine.js` + renderer modules), the file will become significantly more manageable and enable proper unit testing.
- All 17 behaviors have the required fields (`gazeRadius`, `speedMult`, `blinkMult`, `lidOpenness`, `enterDur`, `enterEase`).
- All params exposed in the UI panel match the `params` object.
- All WebSocket commands in `send.py` match the handler in `server.py`.

---

## Status at Review Close

| Area | Status |
|------|--------|
| Drive system (pressure, rule engine, enforcer) | Implemented ✓ |
| Micro-expressions (5 presets, auto-return, gold bar) | Implemented ✓ |
| Transition enforcer + blend-guard | Implemented ✓ |
| Principles off switch | Implemented ✓ |
| `send.py` full command coverage | Implemented ✓ |
| Engine extraction | Not started |
| State broadcast / telemetry mode | Not started |
| Playwright test suite | Not started |
| Hardware adapters (OLED, servo, Pi) | Not started |
